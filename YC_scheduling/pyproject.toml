[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "yard-crane-static-v3"
version = "0.24.0"
description = "Standalone fair-comparison static yard-crane scheduler"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
